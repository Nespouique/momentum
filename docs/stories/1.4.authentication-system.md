# Story 1.4: Authentication System

## Status: Ready for Review

## Story
**As a** user,
**I want** to register and login securely,
**so that** my data is protected and personal.

## Acceptance Criteria
1. Endpoints API : `POST /auth/register`, `POST /auth/login`, `POST /auth/logout`, `GET /auth/me`
2. Mots de passe hashés avec bcrypt
3. JWT tokens générés et validés
4. Middleware d'authentification protégeant les routes privées
5. Page `/login` avec formulaire (email, password)
6. Page `/register` avec formulaire (name, email, password, confirm password, birthDate optionnel)
7. Redirection automatique vers login si non authentifié
8. Stockage sécurisé du token côté client (httpOnly cookie ou secure storage)
9. Validation des inputs côté serveur et messages d'erreur appropriés

## Tasks / Subtasks
- [x] Créer endpoints auth backend (AC: 1, 2, 3)
  - [x] Installer bcrypt, jsonwebtoken, et types
  - [x] Créer `src/services/auth.service.ts`
  - [x] Implémenter hashPassword et comparePassword
  - [x] Implémenter generateToken et verifyToken
  - [x] Créer `src/routes/auth.routes.ts`
  - [x] POST /auth/register - créer utilisateur
  - [x] POST /auth/login - authentifier et retourner token
  - [x] POST /auth/logout - invalider session
  - [x] GET /auth/me - retourner utilisateur courant
- [x] Créer middleware d'auth (AC: 4)
  - [x] Créer `src/middleware/auth.middleware.ts`
  - [x] Extraire et valider JWT du header Authorization
  - [x] Attacher user au request object
  - [x] Retourner 401 si token invalide/absent
- [x] Créer page login (AC: 5)
  - [x] Créer `app/(auth)/login/page.tsx`
  - [x] Formulaire avec React Hook Form + Zod
  - [x] Champs: email, password
  - [x] Gestion loading, erreurs
  - [x] Redirection vers / après succès
- [x] Créer page register (AC: 6)
  - [x] Créer `app/(auth)/register/page.tsx`
  - [x] Formulaire avec validation Zod
  - [x] Champs: name, email, password, confirmPassword
  - [x] Validation: passwords match, email format
  - [x] Redirection vers login après succès
- [x] Implémenter protection des routes (AC: 7)
  - [x] Créer `middleware.ts` Next.js
  - [x] Vérifier présence token pour routes protégées
  - [x] Rediriger vers /login si non authentifié
- [x] Gestion du token côté client (AC: 8)
  - [x] Créer store Zustand pour auth
  - [x] Stocker token en localStorage + state (+ cookie pour middleware)
  - [x] Configurer API client avec Authorization header
- [x] Validation et erreurs (AC: 9)
  - [x] Créer schemas Zod pour validation backend
  - [x] Retourner erreurs formatées (code, message, details)
  - [x] Afficher erreurs côté client

## Dev Notes

### Auth Flow
```
1. Login Form → POST /auth/login
2. Backend vérifie credentials avec bcrypt
3. Génère JWT avec userId et expiration
4. Retourne { user, accessToken, expiresAt }
5. Client stocke token (Zustand + localStorage)
6. Requêtes suivantes incluent Authorization: Bearer <token>
```

### JWT Configuration
```typescript
const JWT_SECRET = process.env.JWT_SECRET;
const JWT_EXPIRES_IN = '7d';

// Payload
{
  userId: string;
  email: string;
  iat: number;
  exp: number;
}
```

### Password Hashing
```typescript
import bcrypt from 'bcrypt';
const SALT_ROUNDS = 12;

// Hash
const hash = await bcrypt.hash(password, SALT_ROUNDS);

// Compare
const isValid = await bcrypt.compare(password, hash);
```

### Zod Schemas
```typescript
// Register
const registerSchema = z.object({
  name: z.string().min(2),
  email: z.string().email(),
  password: z.string().min(8),
  birthDate: z.string().optional(), // YYYY-MM-DD format
});

// Login
const loginSchema = z.object({
  email: z.string().email(),
  password: z.string().min(1),
});
```

### Error Codes
- `INVALID_CREDENTIALS` - email ou password incorrect
- `EMAIL_EXISTS` - email déjà utilisé
- `VALIDATION_ERROR` - données invalides

### Zustand Auth Store
```typescript
interface AuthState {
  user: User | null;
  token: string | null;
  isAuthenticated: boolean;
  login: (email: string, password: string) => Promise<void>;
  logout: () => void;
  checkAuth: () => Promise<void>;
}
```

## Testing
- Tester register avec données valides → user créé
- Tester register avec email existant → erreur
- Tester login avec credentials valides → token retourné
- Tester login avec mauvais password → erreur 401
- Tester accès route protégée sans token → redirection login
- Tester accès route protégée avec token valide → accès autorisé

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-01-17 | 0.1 | Initial story creation | PO Sarah |
| 2026-01-18 | 1.0 | Implementation completed | Dev James |

---

## Dev Agent Record

### Agent Model Used
Claude Opus 4.5

### Debug Log References
- None

### Completion Notes
- Backend auth service with bcrypt (12 rounds) + JWT (7d expiry)
- Full CRUD auth endpoints: register, login, logout, me
- Auth middleware extracts JWT from Authorization header
- Frontend with React Hook Form + Zod validation
- Zustand store with persist middleware (localStorage + cookie for middleware)
- Shadcn UI components (Card, Input, Button, Form, Alert, Label)
- Next.js middleware redirects unauthenticated users to /login
- Custom storage syncs auth state to both localStorage and cookie
- Both apps build successfully (TypeScript strict mode)

### File List
| File | Status |
|------|--------|
| apps/api/src/services/auth.service.ts | Created |
| apps/api/src/schemas/auth.schema.ts | Created |
| apps/api/src/routes/auth.routes.ts | Created |
| apps/api/src/middleware/auth.middleware.ts | Created |
| apps/api/src/index.ts | Modified |
| apps/api/.env | Modified |
| apps/api/.env.example | Modified |
| apps/api/package.json | Modified |
| apps/web/src/lib/validations/auth.ts | Created |
| apps/web/src/lib/api/auth.ts | Created |
| apps/web/src/stores/auth.ts | Created |
| apps/web/src/middleware.ts | Created |
| apps/web/src/app/(auth)/layout.tsx | Created |
| apps/web/src/app/(auth)/login/page.tsx | Created |
| apps/web/src/app/(auth)/register/page.tsx | Created |
| apps/web/src/components/ui/card.tsx | Created |
| apps/web/src/components/ui/input.tsx | Created |
| apps/web/src/components/ui/button.tsx | Created |
| apps/web/src/components/ui/label.tsx | Created |
| apps/web/src/components/ui/form.tsx | Created |
| apps/web/src/components/ui/alert.tsx | Created |
| apps/web/.env.local | Created |
| apps/web/.env.example | Created |
| apps/web/package.json | Modified |
